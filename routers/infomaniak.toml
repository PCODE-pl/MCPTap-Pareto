# mcp-tap-auto-source = collect_routers
is_router = false
reason = "The provider catalog has no verified closed-source frontier models, so it is not classified as a model router."
router_providers_from_models_dir_struct = true
router_providers_from_models_prefixes = false
